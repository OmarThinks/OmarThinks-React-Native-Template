import { View } from 'react-native';
import React from 'react';
import { Text } from '@components';

const Icon = () => {
  return (
    <View>
      <Text>Icon</Text>
    </View>
  );
};

export default Icon;
